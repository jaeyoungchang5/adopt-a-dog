import React from 'react';

interface ISortProps {}

export function Sort(props: ISortProps) {
    /**
     * Support Sort for name, age, breed (single sort parameter)
     */
    return (
        <p>Sort</p>
    )
}